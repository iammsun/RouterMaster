apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"

ext {
    VERSION_NAME = "$ANNOTATION_VERSION"
    ARTIFACT_ID = 'routermaster-annotations'
}
apply from: "../maven.gradle"